'use strict';

var fs = require('fs');
var path = require('path');
var run = require('../utils/run');
var RSVP = require('rsvp');
var rimraf = require('rimraf');

//TODO: Warn/abort if bower.json is dirty
//TODO: Share code
//TODO: Config versions + other packages
//TODO: All change to package/scheme for one command ala ember try 1.10.0 serve
module.exports = function(project, version) {

};
